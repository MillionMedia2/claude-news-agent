# DEPRECATED: This workflow has been replaced by generate-articles.yml
# Keeping this file to prevent GitHub from showing errors.

name: "[DEPRECATED] Generate Article (singular)"

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated. Use generate-articles.yml instead.'
        required: false
        default: 'deprecated'
