# DEPRECATED: This workflow has been replaced by transfer-headlines.yml
# Keeping this file to prevent GitHub from showing errors for missing workflow.
# It will never trigger because it has no events.

name: "[DEPRECATED] Daily Headline Transfer"

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated. Use transfer-headlines.yml instead.'
        required: false
        default: 'deprecated'
